name: Continuous Deployment
 
on: 
  push:
    branches:
      - main
 
jobs:
  deploy:
    runs-on: ubuntu-22.04
 
    steps:
    - name: SSH and deploy app
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_KEY }}
        port: ${{ secrets.SSH_PORT }}
        script: |
          git config --global --add safe.directory ~/APISTORE
          cd ~/APISTORE
          ls -a
          git stash https://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@github.com/cor4kbh1neka/APISTORE.git
          sudo git pull origin main https://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@github.com/cor4kbh1neka/APISTORE.git
          npm install
          npm run migrate up
          sudo pm2 restart api_sepak